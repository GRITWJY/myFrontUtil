/*
归并排序
 */

function mergeSort(arr) {

}

function sortProcess(arr, L, R) {
	// 递归终止条件，就是左右边界索引一样

	 // 找出中间值
	 // 对左侧部分进行递归
	 // 对右侧部分进行递归
	 // 然后利用外派方式进行结合
}

function merge(arr, L, middle, R) {

	// 利用外排方式进行

}
